#include<bits/stdc++.h>
#include<unistd.h>

using namespace std;


int main(){
	char buff[100];
	int fd;
	//fd = open("console",O_RDWR);
	//if(fd >= 3){ close(fd); exit(1);}
	
	char cmd[100];
	scanf("%s",cmd);
	printf("%s",cmd);


return 0;
}

























